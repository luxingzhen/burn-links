{
  "name": "burn-links",
  "version": "0.2.0",
  "description": "A Cloudflare Worker for creating burn-after-reading short links.",
  "main": "src/index.js",
  "scripts": {
    "deploy": "npm run build && wrangler deploy",
    "build": "npm i && wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.2026.0",
    "wrangler": "^4.32.0"
  },
  "dependencies": {
    "itty-router": "^5.0.22",
    "nanoid": "^5.1.5"
  }
}
